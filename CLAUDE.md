@AGENTS.md

Read `docs/status.md` "Now" for the next step, then `README.md` only for what was decided and why.

`exchange/` is the record of past memos to rux-ds and atlas; `docs/` is
its own reasoning. The root holds only those two files, this one and `AGENTS.md`.
